// This file is used to configure JavaScript projects in Visual Studio Code
/*
The compiler options allows me to write imports import { Button } from "@/components/ui/button";
instead of import { Button } from "../../components/ui/button";
this is useful to avoid long relative paths in imports
*/
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
